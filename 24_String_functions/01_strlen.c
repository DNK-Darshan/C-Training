#include <stdio.h>
#include <string.h>

int main()
{
    char s[] = "Hello";

    printf("%ld", strlen(s));

    return 0;
}

/*

strlen() : 
    - Returns the number of characters in a string, excluding the null terminator.

*/
